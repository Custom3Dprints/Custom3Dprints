#include <iostream>
#include <string>
using namespace std;
/*
int main() {
    int operatorsOnCall;
    int callsReceived;
    cout << "Enter operatorsOnCall: ";
    cin>>operatorsOnCall;
    cout << "Enter callsReceived: ";
    cin >> callsReceived;
    if ((operatorsOnCall > 0) || (callsReceived >= 0)){
        cout << operatorsOnCall / callsReceived;
    }else{
        cout << "INVALID";
    }
}
*/

int main2(){
    int operatorsOnCall;
    int callsReceived;
    /*
    cin >> callsReceived;
    cin >> operatorsOnCall;
    if (operatorsOnCall <= 0){
        cout << "INVALID";
    }
    else if (callsReceived < 0){
        cout << "INVALID";
    }
    else{
        cout << callsReceived / operatorsOnCall;
    }
    */
    return 0;
}
/*
int Main2(){
    int operatorsOnCall;
    int callsReceived;
    cin >> callsReceived;
    cin >> operatorsOnCall;
    if (operatorsOnCall <= 0 || callsReceived < 0){
      cout << "INVALID";
    }
    //else if (callsReceived < 0)
    //  cout << "INVALID";
    else{
        cout << callsReceived / operatorsOnCall;
    }
    return 0;
}
*/

int out(){
    main2();
    return 0;
}